node_modules/
dist/
.env
.env.local
.env.*.local
# .env.production is committed on purpose: it holds only the public database URI
# and name, which are baked into the client bundle anyway, and CI needs them.
!.env.example
.DS_Store
*.log
archive/
